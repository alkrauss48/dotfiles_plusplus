name: invoice
root: ~/Code/ruby/invoice
# tmux_options: -2

windows:
  - editor: v
  - server/logs:
      layout: vertical-align
      panes:
        - heroku logs --tail
        - bundle exec ruby app.rb
