name: tea
root: ~/Code/phonegap/teaTimer
# tmux_options: -2

windows:
  - editor:
      layout: 8087,238x60,0,0[238x44,0,0,0,238x15,0,45,5]
      panes:
        - v
        -
  - shell: git remote update origin; git pull --rebase origin
  - console/grunt:
      layout: tiled
      panes:
        - gulp watch
        - cd www; php -S 127.0.0.1:8000
        - mysql.server start
        - ngrok 8000
