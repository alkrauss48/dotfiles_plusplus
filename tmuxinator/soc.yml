name: soc
root: ~/Code/thesocietea/2.0
# tmux_options: -2

windows:
  - editor:
      layout: 8087,238x60,0,0[238x44,0,0,0,238x15,0,45,5]
      panes:
        - v
        -
  - shell: git remote update origin; git pull --rebase origin
  - console/grunt:
      layout: tiled
      panes:
        - grunt
        - php -S localhost:8000
        - mysql.server start; grunt images;
        - ~/Downloads/ngrok 80
